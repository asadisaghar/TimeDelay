# Perform a Lasso regression on the entire dataset
# Features: N_eval*len(windows) encoding residuals of detrend(brighter lightcurve)-detrend(fainter lightcurve)
# labels: dt_true, tau, sigma[, m1, m2]
